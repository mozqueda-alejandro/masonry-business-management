
export enum BlockType {
    SlumpStone,
    Smooth,
    SplitFace
}

export enum FootingType {
    Centric,
    Excentric
}

export enum JobScope {
    Installation = "Installation",
    Repair = "Repair",
    Addition = "Addition",
    Stone = "Stone",
    TemporaryFence = "Temporary Fence",
    Demo = "Demo"
}

export enum SpecialBlockType {
    CornerFlute,
    CornerSplit,
    Flute,
    Netlis,
    Split
}

export enum TopFinish {
    Coping,
    Nellis,
    TwoInchCap,
    None
}

export enum WallRepairPattern {
    Tooth,
    VShape,
    Square
}